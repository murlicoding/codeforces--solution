#include<bits/stdc++.h>
using namespace std;
int main(){
int a,b,c,d;
cin>>a>>b>>c>>d;
unordered_set<int>mp;
    mp.insert(a);
    mp.insert(b);
    mp.insert(c);
    mp.insert(d);
 
    cout << 4 - mp.size();
 
    return 0;
}